DATABASE_URL=
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000 # Base URL of your app
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# Example with SSL mode in the connection string.
# For Supabase on Vercel, prefer the connection string provided by Supabase
# and keep an explicit sslmode, usually ?sslmode=require.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/postgres?sslmode=require
